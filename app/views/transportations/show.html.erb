
    <div><h1 ><%= @transportation.company_name %></h1></div>

    <%= link_to 'Edit', edit_trip_transportation_path(@trip, @transportation), class:"btn btn-outline-primary" %>
     <%= button_to 'Delete', trip_transportation_path(@trip, @transportation), method: :delete, form: {data: {turbo_confirm: "Are you sure you would like to proceed?"}}, class:"btn btn-outline-danger" %>
   </div>